#!/bin/bash
if [[ $EUID -ne 0 ]]; then
   echo -e "\e[1;31mThis program must be run as root (sudo $0)" 
   exit 1
fi
cd /tmp/UX
git reset --hard
git clean -fxd
git pull origin master
cp -rf /tmp/UX/base /
bash /tmp/UX/channels/relselector.sh